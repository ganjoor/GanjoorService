@model GanjooRazor.Pages.MultipleQuotedPoemsPartialModel
@using DNTPersianUtils.Core
@{
    Layout = null;
}

@foreach(var q in Model.GanjoorQuotedPoems)
{
    <partial name="_QuotedPoemPartial" model=@Model.GetQuotedPoemModel(q, Model.PoetImageUrl, Model.PoetNickName) />
}