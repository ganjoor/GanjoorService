@model GanjooRazor.Pages._MultipleQuotedPoemsPartialModel
@using DNTPersianUtils.Core
@{
    Layout = null;
}

@foreach (var q in Model.GanjoorQuotedPoems)
{
    <partial name="_QuotedPoemPartial.cshtml" model=@Model.GetQuotedPoemModel(q, Model.PoetImageUrl, Model.PoetNickName) />
}